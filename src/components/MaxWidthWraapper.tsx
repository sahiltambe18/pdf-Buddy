import React, { ReactNode } from 'react'

const MaxWidthWraapper = ({
    className, children
}:{
    className?:string
    children: ReactNode
}) => { 
  return (
    <div clas>
        MaxWidthWraapper
    </div>
  )
}

export default MaxWidthWraapper